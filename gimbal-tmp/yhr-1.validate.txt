scenario: yhr-1.scenario.json
steps: 30  config.vars: 1  dynamic extract vars: 19

❌ 2 VIOLATION(S):
  - [hallucination] step 26 extract path $.response_body.data.audit[0].audit_id does not resolve in the real response.
  - [hallucination] step 28 extract path $.response_body.data.audit[0].audit_id does not resolve in the real response.

⚠️  132 warning(s):
  - step 6 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 6 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 6 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 6 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 6 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 6 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 7 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 7 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 7 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 7 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 7 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 7 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 9 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 9 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 9 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 9 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 9 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 9 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 10 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 10 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 10 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 10 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 10 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 10 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 12 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 12 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 12 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 12 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 12 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 12 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 13 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 13 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 13 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 13 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 13 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 13 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 14 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 14 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 14 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 14 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 14 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 14 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 15 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 15 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 15 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 15 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 15 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 15 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 16 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 16 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 16 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 16 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 16 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 16 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 17 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 17 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 17 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 17 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 17 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 17 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 18 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 18 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 18 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 18 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 18 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 18 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 19 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 19 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 19 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 19 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 19 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 19 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 20 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 20 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 20 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 20 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 20 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 20 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 21 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 21 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 21 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 21 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 21 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 21 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 22 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 22 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 22 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 22 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 22 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 22 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 23 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 23 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 23 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 23 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 23 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 23 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 24 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 24 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 24 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 24 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 24 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 24 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 25 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 25 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 25 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 25 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 25 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 25 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 26 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 26 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 26 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 26 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 26 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 26 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 27 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 27 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 27 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 27 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 27 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 27 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 28 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 28 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 28 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 28 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 28 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 28 carries browser/transport header 'sec-ch-ua-platform' — strip it.
  - step 29 carries browser/transport header 'Sec-Fetch-Dest' — strip it.
  - step 29 carries browser/transport header 'Sec-Fetch-Mode' — strip it.
  - step 29 carries browser/transport header 'Sec-Fetch-Site' — strip it.
  - step 29 carries browser/transport header 'sec-ch-ua' — strip it.
  - step 29 carries browser/transport header 'sec-ch-ua-mobile' — strip it.
  - step 29 carries browser/transport header 'sec-ch-ua-platform' — strip it.
