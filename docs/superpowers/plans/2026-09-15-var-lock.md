# 变量锁定(过程变量开关)Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 共享变量侧为 config 出身变量提供「过程变量」锁开关;数据集编辑面把未本地放开的锁定列沉底/只读/排除 CSV,支持数据集本地放开(`var_unlocks`)与按变量「恢复默认」;引擎零改动(纯编辑面锁)。

**Architecture:** 锁声明 = 场景级 `definition.config.var_locks: string[]`(definition 纯 dict 旁挂,plate Config `extra="ignore"` 静默剥除);本地放开 = 数据集级 `ComposerDataSet.var_unlocks: string[]`(新 JSON 列,wire 键 `varUnlocks`)。可编辑集 = 未锁定 ∪ 本数据集 var_unlocks。row-wins 运行语义(dispatcher `_compose_scenario`)一行不动。

**Tech Stack:** FastAPI + SQLAlchemy(JSON 列,`init_db` 只 create_all — schema 变更 = 重建库,out-of-band)+ pydantic v2(`_CAMEL` alias)+ Vue 3 `<script setup>` + Element Plus + vitest/@vue/test-utils + pytest。

**Spec:** `docs/superpowers/specs/2026-09-15-var-lock-design.md`(已提交 04eb58a0)。计划从 spec 立论;冲突时以 spec 为准。

## Global Constraints

(摘自 spec §5,实现者必读;每个任务的需求隐含包含本节)

1. **锁实现纯旁挂** — 绝不能「从 config.vars 删变量」或「值改结构化 dict」:palette(`data_set_store._scalar_vars`)一缩,存量行数据下次 PUT 该数据集时 422 `undeclared_var`。
2. **CaseComposerConfig 三处同步**(emitShape / local 初始化 / 入向 watch)— 该文件有回声守卫递归 bug 前科,改完必须跑其既有测试 `CaseComposerConfig.test.ts`。
3. **renameVar 双清单迁移** — `config.var_locks` 与各数据集 `var_unlocks` 都按名存储,都要随改名迁移(应用 + 快照 + 回滚三落点)。
4. **var_unlocks 与 var_locks 交叉态**:本地放开的变量随后被共享侧删除 → 死配置,行为 = 列本就不存在,编辑器静默忽略(dead-keys 提示条先例)。
5. **CSV 导入提示分流**:锁定列跳过是提示(`skippedLocked`)不是报错;绝不落进「未知列」error 路径。
6. 运行链(dispatcher / run_injection / run_materialize / plate_client)零改动 — Task 7 全量回归证明。

**工作树纪律(全程生效):** 选择性暂存(`git add` 仅限任务文件);`gimbal-tmp/`、`reports/test-report.html`、`src/gimbal-plate/gimbal_plate/systems/fin/**`、`probe_ui.js` 永不触碰/提交;commit message 尾加 `Co-Authored-By: Claude Code <noreply@anthropic.com>`;前端测试必须在 `src/gimbal-platform/frontend` 目录跑(根目录跑会捡根配置全挂);后端测试在 `src/gimbal-platform/backend` 目录跑;绝不自动 push。

**测试命令速查:**
- 后端单测:`cd src/gimbal-platform/backend && python -m pytest tests/test_scenario_composer_stores.py -v`(等)
- 前端单测:`cd src/gimbal-platform/frontend && npx vitest run src/views/__tests__/DataSetEditor.varview.test.ts`(等)
- 前端类型:`cd src/gimbal-platform/frontend && npx vue-tsc --noEmit`

---

### Task 1: 后端 — `var_unlocks` 存储往返(模型 + wire + store)

**Files:**
- Modify: `src/gimbal-platform/backend/app/models/composer_data_set.py`
- Modify: `src/gimbal-platform/backend/app/schemas/scenario_composer.py:146-188`
- Modify: `src/gimbal-platform/backend/app/services/data_set_store.py:53-113,186-194`
- Modify: `src/gimbal-platform/backend/app/services/adaptation_service.py:556-572`(两处 DataSetDraft 构造点透传)
- Test: `src/gimbal-platform/backend/tests/test_scenario_composer_stores.py`

**Interfaces:**
- Consumes: 既有 `DataSetDraft` / `ComposerDataSet` / `data_set_store.{create,update,get}`。
- Produces: `ComposerDataSet.var_unlocks: Mapped[list]`(ORM 属性);wire 键 `varUnlocks`(`DataSet.var_unlocks` / `DataSetDraft.var_unlocks: list[str]`,alias `"varUnlocks"`)。Task 2/5 依赖此名。

**背景(为什么动 adaptation_service):** `data_set_store.update` 本任务起写 `row.var_unlocks`;而 `_apply_scenario_op`(renameVar 联动循环)与 `_apply_dataset_op` 构造的 `DataSetDraft` 不带该字段 → 一次适配应用会把存量 var_unlocks 重置为空。本任务先做**原值透传**保绿,Task 2 再加改名迁移。

- [ ] **Step 1: 写失败测试(store 往返)**

追加到 `tests/test_scenario_composer_stores.py` 的 `# ── data_set_store ──` 段尾:

```python
async def test_data_set_var_unlocks_roundtrip(fresh_db) -> None:
    """var_unlocks(变量锁本地放开清单)随创建落库、读取回显、更新重写。"""
    async with _session() as db:
        await scenario_store.create(
            db, _make_draft(config_vars={"x": 0}), owner="alice"
        )
        created = await data_set_store.create(
            db, "sc-test",
            DataSetDraft(name="a", rows=[{"x": 1}], var_unlocks=["x"]),
        )
        assert created.var_unlocks == ["x"]

        fetched = await data_set_store.get(db, created.dataset_id)
        assert fetched.var_unlocks == ["x"]

        updated = await data_set_store.update(
            db, created.dataset_id,
            DataSetDraft(name="a", rows=[{"x": 2}], var_unlocks=[]),
        )
        assert updated.var_unlocks == []
        # wire 键 = varUnlocks(camelCase 出参)
        assert updated.model_dump(by_alias=True)["varUnlocks"] == []


async def test_data_set_var_unlocks_default_empty(fresh_db) -> None:
    """旧客户端不传 var_unlocks → 缺省空清单(不是 None),读写不炸。"""
    async with _session() as db:
        await scenario_store.create(
            db, _make_draft(config_vars={"x": 0}), owner="alice"
        )
        created = await data_set_store.create(
            db, "sc-test", DataSetDraft(name="a", rows=[{"x": 1}])
        )
        assert created.var_unlocks == []
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src/gimbal-platform/backend && python -m pytest tests/test_scenario_composer_stores.py -k var_unlocks -v`
Expected: FAIL(`DataSetDraft` 无 `var_unlocks` 字段 → pydantic extra 行为/TypeError,或断言不等)

- [ ] **Step 3: 模型加列**

`app/models/composer_data_set.py` 在 `rows`/`row_count` 之后(`row_count` 行后)加:

```python
    var_unlocks: Mapped[list] = mapped_column(JSON, default=list)
    # 变量锁本地放开清单(spec 2026-09-15 §3.2):数据集级元数据,
    # 与场景级 config.var_locks 互不影响;引擎不读。
```

无迁移代码 — 平台约定「Schema changes ship as a DB rebuild」(`core/db.py` docstring):create_all 覆盖新库,存量开发库由使用者在部署侧重建。

- [ ] **Step 4: wire 加字段**

`schemas/scenario_composer.py`:`DataSet`(L146-161)`rows` 字段后加:

```python
    var_unlocks: list[str] = Field(default_factory=list, alias="varUnlocks")
```

`DataSetDraft`(L181-188)`rows` 字段后加同一行。(`_CAMEL = ConfigDict(populate_by_name=True, ...)` 已在,Python 侧 `var_unlocks=[...]` 与 wire 侧 `varUnlocks` 双通。)

- [ ] **Step 5: store 携带**

`services/data_set_store.py`:

`create()` 的 `ComposerDataSet(...)` 构造(L71-78)加一行:

```python
        var_unlocks=list(draft.var_unlocks or []),
```

`update()`(L106-109)加一行:

```python
    row.var_unlocks = list(draft.var_unlocks or [])
```

`_to_full_shape()`(L186-194)加:

```python
        varUnlocks=list(row.var_unlocks or []),
```

(`_validate_rows` 与 `_scalar_vars` 不动 — 锁不删 var,palette 不变,spec §4.1.1。)

- [ ] **Step 6: adaptation_service 两处构造点透传(防重置)**

`services/adaptation_service.py` `_apply_scenario_op` renameVar 联动(L558-560)与 `_apply_dataset_op`(L570-572)的 `DataSetDraft(...)` 各加:

```python
                var_unlocks=list(d.var_unlocks or []),
```

- [ ] **Step 7: 跑测试确认通过 + 全量回归**

Run: `cd src/gimbal-platform/backend && python -m pytest tests/test_scenario_composer_stores.py tests/test_adaptation_batches.py -v`
Expected: 全 PASS(含既有测试零修改)

- [ ] **Step 8: Commit**

```bash
git add src/gimbal-platform/backend/app/models/composer_data_set.py \
        src/gimbal-platform/backend/app/schemas/scenario_composer.py \
        src/gimbal-platform/backend/app/services/data_set_store.py \
        src/gimbal-platform/backend/app/services/adaptation_service.py \
        src/gimbal-platform/backend/tests/test_scenario_composer_stores.py
git commit -m "feat(backend): 数据集 var_unlocks 本地放开清单 — 模型/wire/store 往返 + 适配构造点透传

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 2: 后端 — renameVar 双清单迁移(应用 + 快照 + 回滚)

**Files:**
- Modify: `src/gimbal-platform/backend/app/services/adaptation_ops.py:183-206`
- Modify: `src/gimbal-platform/backend/app/services/adaptation_service.py:315-323,549-560,805-842`
- Test: `src/gimbal-platform/backend/tests/test_adaptation_batches.py`

**Interfaces:**
- Consumes: Task 1 的 `ComposerDataSet.var_unlocks` / `DataSetDraft.var_unlocks`。
- Produces: `adaptation_ops.rename_in_list(names: list, src: str, dst: str) -> list`(收敛改名纯函数,Task 内自用)。场景级 `config.var_locks` 经 `apply_to_definition` 重放自动迁移(回滚场景侧无需新代码 — payload 快照整包恢复)。

- [ ] **Step 1: 写失败测试**

追加到 `tests/test_adaptation_batches.py`(import 区已有 `adaptation_service, data_set_store, scenario_store`;补 `from app.services.adaptation_ops import apply_to_definition, rename_in_list`):

```python
# ─── renameVar 双清单迁移(var-lock spec §4.1.2)─────────────────
async def test_rename_var_migrates_lock_lists(fresh_db, plate):
    """renameVar 迁移 config.var_locks + 数据集 var_unlocks(否则按名存储的
    清单陈旧,锁语义静默丢失)。"""
    from tests.helpers import make_draft

    draft = make_draft("sc-batch", steps=_steps(), vars_map={"amount": 100})
    draft["definition"]["config"]["var_locks"] = ["amount"]
    async with await _session() as s:
        await scenario_store.create(
            s, ScenarioDraft.model_validate(draft), owner="alice", owner_id=1,
        )
        await data_set_store.create(s, "sc-batch", DataSetDraft(
            name="主数据集", rows=[{"amount": 5}], var_unlocks=["amount"],
        ))
    await _seed_stamp()
    _install_plate(plate)
    async with await _session() as s:
        detail = await adaptation_service.open_batch(s, endpoint_id=EP, operator_id=1)
        s.add(AdaptationOp(
            batch_id=detail["batchId"], scenario_id="sc-batch", dataset_id=None,
            op_type="renameVar", payload={"from": "amount", "to": "amt"},
            status="pending",
        ))
        await s.commit()
        op_id = (await s.execute(
            select(AdaptationOp).where(AdaptationOp.op_type == "renameVar")
        )).scalar_one().id
        res = await adaptation_service.apply_op(s, op_id)
        scenario = await scenario_store.get_row(s, "sc-batch")
        ds = await data_set_store.get_row(s, "ds-001")
    assert res["status"] == "applied"
    assert scenario.payload["definition"]["config"]["var_locks"] == ["amt"]
    assert ds.rows == [{"amt": 5}]
    assert ds.var_unlocks == ["amt"]


async def test_rename_var_rollback_restores_lock_lists(fresh_db, plate):
    """回滚:场景侧 payload 快照整包恢复(var_locks 随之);数据集侧
    before+ops 重放比对必须覆盖 var_unlocks,恢复写带原清单。"""
    from tests.helpers import make_draft

    draft = make_draft("sc-batch", steps=_steps(), vars_map={"amount": 100})
    draft["definition"]["config"]["var_locks"] = ["amount"]
    async with await _session() as s:
        await scenario_store.create(
            s, ScenarioDraft.model_validate(draft), owner="alice", owner_id=1,
        )
        await data_set_store.create(s, "sc-batch", DataSetDraft(
            name="主数据集", rows=[{"amount": 5}], var_unlocks=["amount"],
        ))
    await _seed_stamp()
    _install_plate(plate)
    async with await _session() as s:
        detail = await adaptation_service.open_batch(s, endpoint_id=EP, operator_id=1)
        s.add(AdaptationOp(
            batch_id=detail["batchId"], scenario_id="sc-batch", dataset_id=None,
            op_type="renameVar", payload={"from": "amount", "to": "amt"},
            status="pending",
        ))
        await s.commit()
        op_id = (await s.execute(
            select(AdaptationOp).where(AdaptationOp.op_type == "renameVar")
        )).scalar_one().id
        await adaptation_service.apply_op(s, op_id)
        result = await adaptation_service.rollback_batch(s, detail["batchId"])
        scenario = await scenario_store.get_row(s, "sc-batch")
        ds = await data_set_store.get_row(s, "ds-001")
    assert not result["conflicts"], result["conflicts"]
    assert scenario.payload["definition"]["config"]["vars"] == {"amount": 100}
    assert scenario.payload["definition"]["config"]["var_locks"] == ["amount"]
    assert ds.rows == [{"amount": 5}]
    assert ds.var_unlocks == ["amount"]


def test_rename_in_list_convergent():
    """纯函数:src 在场且 dst 缺席才改(与 _apply_rename_var 的 vars 键改名
    同一收敛语义);src 缺席 = 已达终态,dst 已在 = 不撞。"""
    assert rename_in_list(["a", "b"], "a", "c") == ["c", "b"]
    assert rename_in_list(["b"], "a", "c") == ["b"]       # src 缺席
    assert rename_in_list(["a", "c"], "a", "c") == ["a", "c"]  # dst 已在


def test_apply_rename_var_migrates_var_locks_in_definition():
    """纯引擎:definition.config.var_locks 键随 renameVar 改名。"""
    definition = {
        "config": {"vars": {"amount": 100}, "var_locks": ["amount"]},
        "steps": [],
    }
    apply_to_definition(definition, {"op": "renameVar", "from": "amount", "to": "amt"})
    assert definition["config"]["vars"] == {"amt": 100}
    assert definition["config"]["var_locks"] == ["amt"]
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src/gimbal-platform/backend && python -m pytest tests/test_adaptation_batches.py -k "lock_lists or rename_in_list" -v`
Expected: FAIL(`rename_in_list` 未定义 / var_locks 迁移断言不等 / 回滚 conflicts 非空)

- [ ] **Step 3: adaptation_ops 加纯函数 + var_locks 迁移**

`services/adaptation_ops.py` 在 `_apply_rename_var` 之前加:

```python
def rename_in_list(names: list, src: str, dst: str) -> list:
    """清单改名(收敛):src 在场且 dst 缺席才改;就地修改并返回同一列表。
    var_locks / var_unlocks 都按名存储,renameVar 双清单迁移共用。"""
    if src in names and dst not in names:
        names[names.index(src)] = dst
    return names
```

`_apply_rename_var` 尾部(vars 键改名块之后)加:

```python
    locks = (definition.get("config") or {}).get("var_locks")
    if isinstance(locks, list):
        rename_in_list(locks, src, dst)
```

- [ ] **Step 4: adaptation_service 三落点**

**(a) `_apply_scenario_op` renameVar 联动循环(L556-560)** — Task 1 的透传行改为迁移:

```python
        for d in ds_rows:
            rows = apply_to_rows(copy.deepcopy(d.rows or []), op_view)
            await data_set_store.update(db, d.dataset_id, DataSetDraft(
                name=d.name, description=d.description, rows=rows,
                var_unlocks=rename_in_list(
                    list(d.var_unlocks or []), op_view["from"], op_view["to"]
                ),
            ))
```

(import 区补 `from .adaptation_ops import apply_to_rows, rename_in_list` — 该文件已 import `apply_to_rows`,在原 import 行上扩。)

**(b) 快照 before_json(L315-323)** — dataset 快照加 varUnlocks:

```python
            before_json={
                "scenarioId": d.scenario_id, "name": d.name,
                "description": d.description,
                "rows": copy.deepcopy(d.rows or []),
                "varUnlocks": list(d.var_unlocks or []),
            },
```

**(c) `_rollback_dataset`(L805-842)** — 重放 + 比对 + 恢复三处:

```python
    before = snap.before_json or {}
    expected_rows = copy.deepcopy(before.get("rows") or [])
    expected_unlocks = list(before.get("varUnlocks") or [])
    try:
        for op in applied_ops:
            op_view = {"op": op.op_type, **(op.payload or {})}
            if (op.op_type == "renameVar"
                    and op.scenario_id == before.get("scenarioId")):
                expected_rows = apply_to_rows(expected_rows, op_view)
                expected_unlocks = rename_in_list(
                    expected_unlocks, op_view["from"], op_view["to"]
                )
            elif op.op_type in DATASET_OPS and op.dataset_id == snap.entity_id:
                expected_rows = apply_to_rows(expected_rows, op_view)
    except (KeyError, ValueError) as e:
        raise _RollbackConflict(f"replay_failed: {e}") from e
    current = {"name": d.name, "description": d.description,
               "rows": d.rows or [], "varUnlocks": list(d.var_unlocks or [])}
    if current != {"name": before.get("name"),
                   "description": before.get("description"),
                   "rows": expected_rows,
                   "varUnlocks": expected_unlocks}:
        raise _RollbackConflict(
            "edited_beyond_batch: current != before+ops replay"
        )
    try:  # before 行键可能已不在调色板(场景侧被冲突跳过)→ 拒写并归冲突
        await data_set_store.update(db, snap.entity_id, DataSetDraft(
            name=before.get("name") or d.name,
            description=before.get("description") or "",
            rows=before.get("rows") or [],
            var_unlocks=list(before.get("varUnlocks") or []),
        ))
    except ValueError as e:
        raise _RollbackConflict(f"restore_failed: {e}") from e
```

(旧快照无 `varUnlocks` 键 → `or []`,与 Task 1 后默认空清单相等 — 旧批次回滚不误报。)

- [ ] **Step 5: 跑测试确认通过 + 适配线回归**

Run: `cd src/gimbal-platform/backend && python -m pytest tests/test_adaptation_batches.py tests/test_scenario_composer_stores.py -v`
Expected: 全 PASS(既有 renameVar / rollback 测试零修改通过)

- [ ] **Step 6: Commit**

```bash
git add src/gimbal-platform/backend/app/services/adaptation_ops.py \
        src/gimbal-platform/backend/app/services/adaptation_service.py \
        src/gimbal-platform/backend/tests/test_adaptation_batches.py
git commit -m "feat(backend): renameVar 双清单迁移 — config.var_locks + 数据集 var_unlocks(应用/快照/回滚三落点)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 3: 前端 — ConfigView 类型 + CaseComposerConfig 锁开关(三处同步)

**Files:**
- Modify: `src/gimbal-platform/frontend/src/types/plate.ts:395-404`
- Modify: `src/gimbal-platform/frontend/src/components/composer/CaseComposerConfig.vue`
- Test: `src/gimbal-platform/frontend/src/components/composer/__tests__/CaseComposerConfig.test.ts`

**Interfaces:**
- Consumes: 无(首个前端任务)。
- Produces: `ConfigView.var_locks?: string[]`;emit 形状 = 仅当存在锁定行时携带 `var_locks`(空则省略键)。Task 5 的 DataSetEditor 读 `draft.definition.config.var_locks`。

- [ ] **Step 1: 写失败测试**

追加到 `CaseComposerConfig.test.ts`:

```typescript
describe('CaseComposerConfig — 变量锁(var-lock spec §2)', () => {
  const LOCKED_CFG = () => ({
    ...makeConfig(),
    vars: { 'fin.env': 'qa', 'fin.amount': 100 },
    var_locks: ['fin.env'],
  }) as ConfigView

  /** 行级锁按钮(c-kv-row 内;键输入框之前定位不可靠,按 class 找) */
  const lockBtns = (w: ReturnType<typeof mountWithParent>['w']) =>
    w.findAll('button.c-kv-lock')

  it('CFG-LOCK-1: 外部 config 带 var_locks → 对应行锁钮呈开态', async () => {
    const { w } = mountWithParent(LOCKED_CFG())
    await flush()
    const btns = lockBtns(w)
    expect(btns.length).toBe(2)                       // 一变量一钮
    expect(btns[0].classes()).toContain('is-on')      // fin.env(首行)
    expect(btns[1].classes()).not.toContain('is-on')  // fin.amount
  })

  it('CFG-LOCK-2: 点锁钮 → 父 config.var_locks 增/删该键', async () => {
    const initial = LOCKED_CFG()
    const { w, config } = mountWithParent(initial)
    await flush()
    // 锁上 fin.amount(第 2 行)
    await lockBtns(w)[1].trigger('click')
    await flush()
    expect(config.value.var_locks).toEqual(['fin.env', 'fin.amount'])
    // 再解锁 fin.env(第 1 行)
    await lockBtns(w)[0].trigger('click')
    await flush()
    expect(config.value.var_locks).toEqual(['fin.amount'])
  })

  it('CFG-LOCK-3: 全部解锁 → var_locks 键省略(非空数组),不残留空清单', async () => {
    const { w, config } = mountWithParent(LOCKED_CFG())
    await flush()
    await lockBtns(w)[0].trigger('click')
    await flush()
    expect(config.value.var_locks).toBeUndefined()
    expect('var_locks' in config.value).toBe(false)
  })

  it('CFG-LOCK-4: 锁状态不参与 vars 值折叠(锁不是值形状变化)', async () => {
    const { w, config } = mountWithParent(LOCKED_CFG())
    await flush()
    expect(config.value.vars).toEqual({ 'fin.env': 'qa', 'fin.amount': 100 })
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src/gimbal-platform/frontend && npx vitest run src/components/composer/__tests__/CaseComposerConfig.test.ts`
Expected: FAIL(`button.c-kv-lock` 不存在 / `var_locks` undefined)

- [ ] **Step 3: 类型加字段**

`types/plate.ts` `ConfigView`(L396-404)加:

```typescript
  /** 变量锁清单(平台旁挂,plate Config extra=ignore 静默剥除)— 锁定 =
   *  过程变量,数据集编辑面默认沉底/只读(var-lock spec §3.1) */
  var_locks?: string[]
```

- [ ] **Step 4: CaseComposerConfig 三处同步 + 锁钮**

**(a) VarRow 与初始构建(L356-359):**

```typescript
interface VarRow { key: string; value: unknown; locked?: boolean }
const varsRows = ref<VarRow[]>(
  Object.entries(props.modelValue?.vars ?? {}).map(([key, value]) => ({
    key, value,
    locked: props.modelValue?.var_locks?.includes(key) ?? false,
  }))
)
```

**(b) 模板 c-kv-row(L178-193)** — 删除钮之前加锁钮:

```html
            <button
              class="c-kv-lock"
              :class="{ 'is-on': v.locked }"
              :title="v.locked
                ? '已锁定为过程变量 — 数据集编辑面默认隐藏该列(可在数据集本地放开)'
                : '锁定为过程变量(数据集编辑面默认隐藏该列)'"
              @click="v.locked = !v.locked"
            >{{ v.locked ? '🔒' : '🔓' }}</button>
```

**(c) emitShape(L404-420)** — 尾部加条件键(**只在非空时携带**:旧 draft 无 var_locks,恒发空数组会让回声守卫比较恒不等、每次外部写触发一轮重建/emit — 该文件递归 bug 前科,见 L396-403 注释):

```typescript
  const varLocks = varsRows.value.filter(r => r.locked && r.key).map(r => r.key)
  return {
    setup: [...setupList.value],
    teardown: [...teardownList.value],
    services: Object.fromEntries(
      serviceRows.value.filter(r => r.alias).map(r => [r.alias, r.baseUrl])
    ),
    users: local.users || {},
    timePolicy: local.timePolicy,
    retry: local.retry,
    vars: varsDict,
    ...(varLocks.length ? { var_locks: varLocks } : {}),
  }
```

**(d) 入向 watch(L422-439)** — 尾部 varsRows 重建行改为:

```typescript
  const locks = v.var_locks ?? []
  varsRows.value = Object.entries(v.vars || {}).map(
    ([key, value]) => ({ key, value, locked: locks.includes(key) })
  )
```

**(e) addVar(L460):**

```typescript
function addVar() { varsRows.value.push({ key: '', value: '', locked: false }) }
```

**(f) 样式**(scoped style 区,c-kv-del 附近):

```css
.c-kv-lock {
  border: none; background: none; cursor: pointer; font-size: 13px;
  line-height: 1; padding: 2px 4px; opacity: 0.45;
}
.c-kv-lock.is-on { opacity: 1; }
.c-kv-lock:hover { opacity: 1; }
```

- [ ] **Step 5: 跑测试确认通过(含既有零回归)**

Run: `cd src/gimbal-platform/frontend && npx vitest run src/components/composer/__tests__/CaseComposerConfig.test.ts && npx vue-tsc --noEmit`
Expected: 全 PASS / 0 类型错

- [ ] **Step 6: Commit**

```bash
git add src/gimbal-platform/frontend/src/types/plate.ts \
        src/gimbal-platform/frontend/src/components/composer/CaseComposerConfig.vue \
        src/gimbal-platform/frontend/src/components/composer/__tests__/CaseComposerConfig.test.ts
git commit -m "feat(frontend): 共享变量卡行级锁开关 — ConfigView.var_locks 三处同步(emitShape/初始化/入向 watch)

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 4: 前端 — CSV 导入锁定列跳过提示

**Files:**
- Modify: `src/gimbal-platform/frontend/src/utils/csv-dataset.ts:80-93,109-204`
- Test: `src/gimbal-platform/frontend/src/utils/__tests__/csv-dataset.test.ts`

**Interfaces:**
- Consumes: 无。
- Produces: `CsvImportInput.lockedVars?: string[]`;`CsvImportResult.skippedLocked: string[]`。Task 5 的 DataSetEditor 消费。导出侧**不改本文件** — 排除由 DataSetEditor 过滤列实现(一次排除,导出/导入同源)。

- [ ] **Step 1: 写失败测试**

追加到 `utils/__tests__/csv-dataset.test.ts`(沿用该文件既有的 `makeColumn` / 造数助手;若名不同以文件内现名为准):

```typescript
describe('importDataSetCsv — 锁定列跳过(var-lock spec §2 CSV 行)', () => {
  const cols = [
    { stepIndex: 0, source: 'body', field: 'amount', kind: 'var', varName: 'amount', baseline: '1' },
    { stepIndex: 0, source: 'body', field: 'env', kind: 'var', varName: 'env', baseline: 'qa' },
  ] as any[]

  const csv = [
    '__case_name,amount,env,ghost',
    '(baseline),1,qa,',
    'data-1,5,qa,x',
  ].join('\n')

  it('锁定列跳过写键 + 计入 skippedLocked;不进未知列 error', () => {
    const res = importDataSetCsv({
      fileText: csv, columns: cols, rows: [], mode: 'replace',
      lockedVars: ['env'],
    })
    expect(res.errors).toEqual(['未知列: ghost(不在场景变量调色板中)'])
    expect(res.skippedLocked).toEqual(['env'])
    // env 在 palette 内(锁定≠删除)→ 不报未知;但数据行不写 env 键
    expect(res.rows).toEqual([{ amount: '5' }])
  })

  it('不传 lockedVars → 行为与旧版完全一致(锁定分流不激活)', () => {
    const res = importDataSetCsv({
      fileText: csv, columns: cols, rows: [], mode: 'replace',
    })
    expect(res.errors).toEqual(['未知列: ghost(不在场景变量调色板中)'])
    expect(res.skippedLocked).toEqual([])
    expect(res.rows).toEqual([{ amount: '5', env: 'qa' }])  // == baseline → 继承不写键
  })
})
```

(第二例 `env: 'qa'` 与 baseline 相等 → 按继承规则本就不写键;如要断言「未锁定锁定列正常参与覆盖」,把 csv 数据行 env 值改为 `prod` 后断言 `env: 'prod'` — 保留一个显式覆盖例:)

```typescript
  it('未锁定的列正常参与覆盖语义', () => {
    const res = importDataSetCsv({
      fileText: '__case_name,amount\n(baseline),1\ndata-1,9\n',
      columns: [cols[0]], rows: [], mode: 'replace',
      lockedVars: ['env'],
    })
    expect(res.rows).toEqual([{ amount: '9' }])
  })
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src/gimbal-platform/frontend && npx vitest run src/utils/__tests__/csv-dataset.test.ts`
Expected: FAIL(`skippedLocked` 不在结果形状上 → undefined 断言失败)

- [ ] **Step 3: 实现**

`CsvImportInput` 加:

```typescript
  /** 锁定且未本地放开的变量名 — 这些列跳过写键并计入 skippedLocked
   *  (提示不是报错,与未知列 error 分流;var-lock spec §5.5) */
  lockedVars?: string[]
```

`CsvImportResult` 加:

```typescript
  /** 被跳过的锁定列名(去重,首现序)— 调用方据此弹「N 列已锁定,已忽略」 */
  skippedLocked: string[]
```

`importDataSetCsv` 内:

(a) header 校验循环(L150-154)分流 — 锁定列不进未知列 error:

```typescript
  const lockedSet = new Set(input.lockedVars ?? [])
  const skippedLocked: string[] = []
  for (const name of headerCols) {
    if (lockedSet.has(name)) {
      if (!skippedLocked.includes(name)) skippedLocked.push(name)
      continue
    }
    if (!paletteByVar.has(name)) {
      errors.push(`未知列: ${name}(不在场景变量调色板中)`)
    }
  }
```

(b) 数据行列循环(L162-170)跳过锁定列写键:

```typescript
      if (lockedSet.has(colName)) continue
```

(加在 `if (!paletteByVar.has(colName)) continue` 之后。)

(c) 两个提前 return(L123)与尾部 return 补齐新字段:

```typescript
    return { rows: [], caseNames: [], errors: ['CSV 为空'], skippedLocked: [] }
```

```typescript
  return { rows: outRows, caseNames: outCaseNames, errors, skippedLocked }
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cd src/gimbal-platform/frontend && npx vitest run src/utils/__tests__/csv-dataset.test.ts && npx vue-tsc --noEmit`
Expected: 全 PASS(CsvImportResult 新必填字段 `skippedLocked` 若造成既有消费方类型错,DataSetEditor 调用点在 Task 5 改;若 vue-tsc 在本任务报 DataSetEditor 处错,把该字段收窄为 `skippedLocked?: string[]` 并在 Task 5 保持可选 — 测试断言不变)

- [ ] **Step 5: Commit**

```bash
git add src/gimbal-platform/frontend/src/utils/csv-dataset.ts \
        src/gimbal-platform/frontend/src/utils/__tests__/csv-dataset.test.ts
git commit -m "feat(frontend): CSV 导入锁定列分流 — lockedVars 跳过写键 + skippedLocked 提示

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 5: 前端 — DataSetEditor 锁定列(沉底/只读/本地放开/恢复默认/TSV 拒写/CSV 链)

**Files:**
- Modify: `src/gimbal-platform/frontend/src/types/scenario-composer.ts:85-108`
- Modify: `src/gimbal-platform/frontend/src/views/DataSetEditor.vue`
- Test: `src/gimbal-platform/frontend/src/views/__tests__/DataSetEditor.varlock.test.ts`(新建;骨架抄 `DataSetEditor.varview.test.ts` 的 api mock + mountEditor 工厂)

**Interfaces:**
- Consumes: Task 3 `ConfigView.var_locks`;Task 4 `CsvImportInput.lockedVars` / `CsvImportResult.skippedLocked`;Task 1 wire 键 `varUnlocks`。
- Produces: `DataSet.var_unlocks?: string[]` / `DataSetDraft.var_unlocks?: string[]`;`unlockVar/relockVar/resetVarDefault(name: string)` 行为契约(Task 6 面板事件复用 `resetVarDefault` 与 unlock 状态翻转)。

- [ ] **Step 1: 写失败测试(新文件)**

`src/gimbal-platform/frontend/src/views/__tests__/DataSetEditor.varlock.test.ts`:

```typescript
/**
 * DataSetEditor 变量锁定(var-lock spec §2):
 * 锁定未放开的列沉底/只读/带锁徽标;本地放开写 var_unlocks;
 * 恢复默认 = 清全部行键 + 撤放开;TSV 拒写;CSV 导出排除 + 导入跳过提示。
 * 骨架(api mock + mountEditor)复制 DataSetEditor.varview.test.ts。
 */
import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest'
import { flushPromises, mount } from '@vue/test-utils'
import ElementPlus from 'element-plus'
import { createPinia, setActivePinia } from 'pinia'

const routerMock = vi.hoisted(() => ({ push: vi.fn() }))
vi.mock('vue-router', () => ({
  useRoute: () => ({ params: { scenarioId: 'sc-ds', datasetId: 'ds-1' } }),
  useRouter: () => ({ push: routerMock.push }),
  createRouter: () => ({ beforeEach: () => {}, push: vi.fn(), replace: vi.fn() }),
  createWebHistory: () => ({}),
}))
vi.mock('@/api/query-views', () => ({
  fetchQueryViewIndex: vi.fn(async () => []),
  fetchQueryViewRows: vi.fn(async () => ({ view: '', rows: [], truncated: false, fetched_at: '', cached: false, stale: false })),
}))

import * as api from '@/api/scenario-composer'
import DataSetEditor from '@/views/DataSetEditor.vue'

/** env(锁定过程变量)+ amount(业务变量);env 有历史行覆盖。 */
const DEF_LOCKED = {
  kind: 'scenario', scenarioId: 'sc-ds', meta: { name: 's' },
  config: { vars: { env: 'qa', amount: 100 }, var_locks: ['env'] },
  steps: [
    { kind: 'step', description: '下单', api: { headers: {} }, request: { kind: 'request', body: { env: '${var.env}', amount: '${var.amount}' } }, strategy: [] },
  ],
}

/** mountEditor:var_unlocks 由 getDataSet 回显(默认 [])*/
async function mountEditor(
  def: typeof DEF_LOCKED,
  rows: Array<Record<string, any>> = [{ amount: '-1', env: 'prod' }],
  varUnlocks: string[] = [],
) {
  vi.spyOn(api, 'getScenarioDraft').mockResolvedValue(
    { definition: def, orchestration: { steps: def.steps.map((s: any) => ({ name: s.description })), resourceMeta: {} } } as any,
  )
  vi.spyOn(api, 'updateScenario').mockResolvedValue({} as any)
  vi.spyOn(api, 'getDataSet').mockResolvedValue(
    { name: 'n', description: '', rows, var_unlocks: varUnlocks } as any,
  )
  vi.spyOn(api, 'getFullEndpoint').mockImplementation(async (eid: string) => ({
    id: eid, request: { declarations: [] },
  } as any))
  const w = mount(DataSetEditor, { global: { plugins: [ElementPlus] } })
  await flushPromises()
  return w
}

beforeEach(() => { setActivePinia(createPinia()); routerMock.push.mockReset() })
afterEach(() => { vi.restoreAllMocks() })

describe('DataSetEditor — 锁定列编辑面(var-lock)', () => {
  it('LOCK-1: 锁定列沉底 + 锁徽标;未锁定列在前', async () => {
    const w = await mountEditor(DEF_LOCKED)
    const heads = w.findAll('.row-field .th-data').map((t) => t.text())
    expect(heads[0]).toContain('amount')            // 业务变量在前
    expect(heads[1]).toContain('env')               // 锁定列沉底
    expect(heads[1]).toContain('🔒')                 // 锁徽标
    w.unmount()
  })

  it('LOCK-2: 锁定格只读;历史覆盖格带信息提示', async () => {
    const w = await mountEditor(DEF_LOCKED)
    const cells = w.findAll('input.data-cell-input')
    // 列序 [amount, env] → env 格 = 第 2 数据列
    expect(cells[1].attributes('readonly')).toBeDefined()
    const envTd = w.findAll('.row-data td.td-data')[1]
    expect(envTd.attributes('title')).toContain('已锁定为过程变量,本数据集使用自有值')
    w.unmount()
  })

  it('LOCK-3: 列头「放开」→ var_unlocks 增员 + 格可编辑;「收回」逆操作', async () => {
    const w = await mountEditor(DEF_LOCKED)
    const unlockBtn = w.findAll('button.col-unlock').find((b) => b.text().includes('放开'))
    expect(unlockBtn).toBeTruthy()
    await unlockBtn!.trigger('click')
    await flushPromises()
    expect((w.vm as any).varUnlocks).toEqual(['env'])
    expect(w.findAll('input.data-cell-input')[1].attributes('readonly')).toBeUndefined()
    const relockBtn = w.findAll('button.col-unlock').find((b) => b.text().includes('收回'))
    await relockBtn!.trigger('click')
    await flushPromises()
    expect((w.vm as any).varUnlocks).toEqual([])
    w.unmount()
  })

  it('LOCK-4: 恢复默认 = 清该变量全部行键 + 撤销本地放开', async () => {
    const w = await mountEditor(DEF_LOCKED, [{ amount: '-1', env: 'prod' }, { amount: '2', env: 'staging' }], ['env'])
    const resetBtn = w.findAll('button.col-reset').find((b) => b.text().includes('恢复默认'))
    expect(resetBtn).toBeTruthy()
    await resetBtn!.trigger('click')
    await flushPromises()
    expect((w.vm as any).rows).toEqual([{ amount: '-1' }, { amount: '2' }])
    expect((w.vm as any).varUnlocks).toEqual([])
    w.unmount()
  })

  it('LOCK-5: TSV 粘贴对未放开锁定列拒写(不落键,提示)', async () => {
    const w = await mountEditor(DEF_LOCKED)
    const envCell = w.findAll('input.data-cell-input')[1]
    const evt = new Event('paste', { bubbles: true, cancelable: true })
    Object.defineProperty(evt, 'clipboardData', { value: { getData: () => 'a\nb' } })
    await envCell.element.dispatchEvent(evt)
    await flushPromises()
    expect((w.vm as any).rows).toEqual([{ amount: '-1', env: 'prod' }])  // 未动
    w.unmount()
  })

  it('LOCK-6: CSV 导出排除锁定列(本地放开的不排除)', async () => {
    const w = await mountEditor(DEF_LOCKED)
    const csv = await import('@/utils/csv-dataset')
    const exportSpy = vi.spyOn(csv, 'exportDataSetCsv').mockImplementation(() => {})
    const btn = w.findAll('button').find((b) => b.text().includes('导出 CSV'))
    await btn!.trigger('click')
    await flushPromises()
    const args = exportSpy.mock.calls[0][0] as any
    expect(args.columns.map((c: any) => c.varName)).toEqual(['amount'])   // env 排除
    // 本地放开后 → 导出带回
    const unlockBtn = w.findAll('button.col-unlock').find((b) => b.text().includes('放开'))
    await unlockBtn!.trigger('click')
    await flushPromises()
    exportSpy.mockClear()
    await btn!.trigger('click')
    await flushPromises()
    expect((exportSpy.mock.calls[0][0] as any).columns.map((c: any) => c.varName))
      .toEqual(['amount', 'env'])
    w.unmount()
  })

  it('LOCK-7: CSV 导入带锁定列(旧模板)→ 跳过写键 + skippedLocked 提示分流', async () => {
    const w = await mountEditor(DEF_LOCKED)
    const csv = await import('@/utils/csv-dataset')
    const importSpy = vi.spyOn(csv, 'importDataSetCsv').mockReturnValue({
      rows: [{ amount: '9' }], caseNames: ['data-1'], errors: [], skippedLocked: ['env'],
    } as any)
    const file = new File(['x'], 't.csv', { type: 'text/csv' })
    await (w.vm as any).onImportCsv(file)
    await flushPromises()
    expect(importSpy.mock.calls[0][0].lockedVars).toEqual(['env'])
    expect((w.vm as any).rows).toEqual([{ amount: '9' }])   // env 键未被写入
    w.unmount()
  })

  it('LOCK-8: 保存数据集携带 var_unlocks;载入回显', async () => {
    const w = await mountEditor(DEF_LOCKED, [{ amount: '-1', env: 'prod' }], ['env'])
    expect((w.vm as any).varUnlocks).toEqual(['env'])
    const saveSpy = vi.spyOn(api, 'updateDataSet').mockResolvedValue({} as any)
    const store = (w.vm as any).store
    store.saveDataSet = vi.fn(async (_sid: string, _did: string | null, draft: any) => draft)
    const btn = w.findAll('button').find((b) => b.text().includes('保存数据集'))
    await btn!.trigger('click')
    await flushPromises()
    expect(store.saveDataSet).toHaveBeenCalled()
    const draft = store.saveDataSet.mock.calls[0][2]
    expect(draft.var_unlocks).toEqual(['env'])
    saveSpy.mockRestore()
    w.unmount()
  })
})
```

(注:LOCK-8 中 `store.saveDataSet` mock 拦截在 pinia 层,直接断言 draft 形状;`saveDataSet` 真实通路 Task 1 后端已覆盖。若 `(w.vm as any).store` 在 `<script setup>` 下不可达,改用 `useScenarioComposerStore()` 在测试侧取同一 pinia 实例并 spyOn 其 `saveDataSet`。)

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src/gimbal-platform/frontend && npx vitest run src/views/__tests__/DataSetEditor.varlock.test.ts`
Expected: 全 FAIL(无沉底/锁钮/只读逻辑)

- [ ] **Step 3: 类型加字段**

`types/scenario-composer.ts`:`DataSet`(L85-92)与 `DataSetDraft`(L104-108)各加:

```typescript
  /** 变量锁本地放开清单(var-lock spec §3.2)— 数据集级元数据,
   *  与场景级 config.var_locks 互不影响 */
  var_unlocks?: string[]
```

- [ ] **Step 4: DataSetEditor 实现**

**(a) 状态与谓词**(script setup,`varChoice` 定义之前插入):

```typescript
// ── 变量锁定(var-lock spec §2)─────────────────────────────
/** 数据集本地放开清单(数据集级元数据,随「保存数据集」落库) */
const varUnlocks = ref<string[]>([])
/** 场景级锁清单(definition.config.var_locks 旁挂,只读消费) */
const varLocks = computed<string[]>(() =>
  (draft.value?.definition?.config?.var_locks ?? []) as string[])
/** 锁定且未本地放开 = 编辑面隐藏态(沉底/只读/CSV 排除) */
function isLockedHidden(name: string): boolean {
  return varLocks.value.includes(name) && !varUnlocks.value.includes(name)
}
/** 声明锁定(含已本地放开 — 「收回」钮与面板放开开关的判定面) */
function isLockDeclared(name: string): boolean {
  return varLocks.value.includes(name)
}
/** 本地放开/收回:写本数据集 var_unlocks,永不写场景配置(spec §2 边界) */
function unlockVar(name: string) {
  if (!varUnlocks.value.includes(name)) varUnlocks.value.push(name)
}
function relockVar(name: string) {
  varUnlocks.value = varUnlocks.value.filter(n => n !== name)
}
/** 恢复默认(全覆盖撤销):清该变量全部行键 + 撤销本地放开 → 回到
 *  「锁定、继承共享侧值」的默认态(spec §2) */
function resetVarDefault(name: string) {
  for (const r of rows.value) delete r[name]
  varUnlocks.value = varUnlocks.value.filter(n => n !== name)
  rowsDirty.value = true
  ElMessage.success(`已恢复默认:「${name}」全部覆盖已清,回到共享侧配置`)
}
```

**(b) varColumns 沉底排序**(L437-469 的 computed 尾部,`return [...declared, ...undeclared].map(...)` 改为):

```typescript
  const cols = [...declared, ...undeclared].map((name) => {
    /* …原 map 体不动… */
  })
  // 锁定未放开的列沉底(组内保持声明序;spec §2 数据集网格行)
  return [
    ...cols.filter(c => !isLockedHidden(c.varName)),
    ...cols.filter(c => isLockedHidden(c.varName)),
  ]
```

**(c) 列头(row-field 的 th,L153-185)** — varName 徽标之后加锁面(锁徽标 + 放开/收回 + 恢复默认):

```html
              <span
                v-if="isLockDeclared(col.varName)"
                class="lock-col-badge"
                title="过程变量 · 默认用共享侧配置(运行取值规则不变)"
              >🔒</span>
              <button
                v-if="isLockDeclared(col.varName)"
                type="button"
                class="col-unlock"
                :title="isLockedHidden(col.varName)
                  ? '本数据集放开该列编辑(不碰共享侧、不碰其他数据集)'
                  : '收回本地放开 — 回到锁定只读态'"
                @click.stop="isLockedHidden(col.varName) ? unlockVar(col.varName) : relockVar(col.varName)"
              >{{ isLockedHidden(col.varName) ? '放开' : '收回' }}</button>
              <button
                v-if="isLockDeclared(col.varName) && rows.some(r => Object.prototype.hasOwnProperty.call(r, col.varName))"
                type="button"
                class="col-reset"
                title="清掉本数据集所有行里该变量的覆盖键 + 撤销本地放开"
                @click.stop="resetVarDefault(col.varName)"
              >恢复默认</button>
```

(th 的 `:class` 数组补 `'col-locked'`:加 `isLockedHidden(col.varName) ? 'col-locked' : ''`。)

**(d) 数据格(L232-248)** — input 加 readonly + 守卫;td 信息提示:

```html
            <td
              v-for="col in varColumns"
              :key="`c:${i}:${col.varName}`"
              :class="['td-data', cellClass(row, bcOf(col)), col.expect ? 'col-expect' : '',
                       isLockedHidden(col.varName) ? 'col-locked' : '']"
              :title="isLockedHidden(col.varName) && Object.prototype.hasOwnProperty.call(row, col.varName)
                ? '已锁定为过程变量,本数据集使用自有值 — 可「恢复默认」回到共享侧配置'
                : (col.baselineStructured ? col.baselineJson : col.baseline)"
            >
              <input
                :value="row[col.varName] ?? ''"
                class="data-cell-input"
                :readonly="isLockedHidden(col.varName)"
                :placeholder="col.baselineStructured ? `按基线(${col.baseline})` : col.baseline"
                @input="(e: Event) => onCellInput(i, bcOf(col), (e.target as HTMLInputElement).value)"
                @paste="(e: ClipboardEvent) => onCellPaste(e, bcOf(col), i)"
              />
            </td>
```

**(e) onCellInput / onCellPaste 守卫**(面板 set-cell 与粘贴两条写路径都堵;readonly UI 是第一道,这里是语义守卫):

`onCellInput` 函数首部加:

```typescript
  if (col.varName && isLockedHidden(col.varName)) return
```

`onCellPaste` 在 `if (!col.varName) return` 之后加:

```typescript
  if (isLockedHidden(col.varName)) {
    e.preventDefault()
    ElMessage.warning(`「${col.varName}」已锁定为过程变量 — 如需编辑请先在列头「放开」`)
    return
  }
```

**(f) CSV 链** — 导出排除 + 导入分流(L792-829)。新增导出专用列宇宙:

```typescript
/** CSV 导出列宇宙:排除未本地放开的锁定列(spec §2 CSV 导出行)。
 *  本地放开的列照常参与。导入 columns 用同源(回导模板即导出形状)。 */
const exportVarColumns = computed<BaselineColumn[]>(() =>
  csvVarColumns.value.filter(c => !isLockedHidden(c.varName!)))
```

`onExportCsv` 的 `columns: csvVarColumns.value` → `columns: exportVarColumns.value`,`descriptions` 映射源同步改 `exportVarColumns.value`。

`onImportCsv`:`columns: csvVarColumns.value` 保持全量宇宙(旧模板可含锁定列),加锁定列集与提示分流:

```typescript
    const result = importDataSetCsv({
      fileText: text,
      columns: csvVarColumns.value,
      lockedVars: varColumns.value.filter(c => isLockedHidden(c.varName)).map(c => c.varName),
      rows: rows.value.map(toApiRow),
      caseNames: caseNames.value,
      mode: 'merge-by-name',
    })
    if (result.errors.length) {
      ElMessage.warning(`CSV 导入有问题:${result.errors.join('; ')}`)
    } else {
      ElMessage.success(`CSV 导入成功(共 ${result.rows.length} 条数据)`)
    }
    if (result.skippedLocked?.length) {
      ElMessage.info(`${result.skippedLocked.length} 列已锁定,已忽略:${result.skippedLocked.join(', ')}`)
    }
```

**(g) 载入/保存**(onMounted L832-850 / onSaveRows L752-774):

onMounted 既有库分支加:

```typescript
      varUnlocks.value = [...(full.var_unlocks ?? [])]
```

(`new` 分支不写 — ref 初始即 `[]`。)

onSaveRows 的 draft 对象加:

```typescript
      var_unlocks: [...varUnlocks.value],
```

(pinia `saveDataSet` 是 draft 透传,后端 Task 1 已接。)

**(h) 样式**(scoped):

```css
/* 锁定列(var-lock):沉底组的表头/格子灰化 + 锁徽标 */
.data-table th.col-locked { background: #f8fafc; color: #94a3b8; }
.data-table .row-baseline td.col-locked { color: #94a3b8; }
.lock-col-badge { margin-left: 4px; font-size: 11px; }
.col-unlock, .col-reset {
  border: none; background: none; cursor: pointer; font-size: 10px;
  color: #64748b; padding: 0 3px; margin-left: 4px;
}
.col-unlock:hover, .col-reset:hover { color: var(--accent); text-decoration: underline; }
```

- [ ] **Step 5: 跑测试确认通过 + varview/palette 零回归 + 类型**

Run: `cd src/gimbal-platform/frontend && npx vitest run src/views/__tests__/DataSetEditor.varlock.test.ts src/views/__tests__/DataSetEditor.varview.test.ts src/views/__tests__/DataSetEditor.palette.test.ts && npx vue-tsc --noEmit`
Expected: 全 PASS / 0 类型错

- [ ] **Step 6: Commit**

```bash
git add src/gimbal-platform/frontend/src/types/scenario-composer.ts \
        src/gimbal-platform/frontend/src/views/DataSetEditor.vue \
        src/gimbal-platform/frontend/src/views/__tests__/DataSetEditor.varlock.test.ts
git commit -m "feat(frontend): 数据集编辑器锁定列 — 沉底/只读/本地放开/恢复默认/TSV 拒写/CSV 导出排除+导入跳过

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 6: 前端 — VariableDetailPanel 锁定态(放开开关 + 行值只读 + 恢复默认)

**Files:**
- Modify: `src/gimbal-platform/frontend/src/components/dataset/VariableDetailPanel.vue`
- Modify: `src/gimbal-platform/frontend/src/views/DataSetEditor.vue`(接线)
- Test: `src/gimbal-platform/frontend/src/components/dataset/__tests__/VariableDetailPanel.test.ts`

**Interfaces:**
- Consumes: Task 5 的 `unlockVar/relockVar/resetVarDefault` 与 `varLocks/varUnlocks`。
- Produces: 面板 props `locked?: boolean` / `unlocked?: boolean`;emits `'toggle-unlock': [name: string, on: boolean]` / `'reset-default': [name: string]`。基线卡行为不变(锁不约束共享侧,spec §2 边界)。

- [ ] **Step 1: 写失败测试**

追加到 `components/dataset/__tests__/VariableDetailPanel.test.ts`(沿用该文件既有的 mount 工厂/props 造数;此处给出断言体,挂载助手名以文件内现名为准 — 若现名不同按现名替换):

```typescript
describe('VariableDetailPanel — 锁定态(var-lock spec §4.2.4)', () => {
  it('PANEL-LOCK-1: locked 且未放开 → 顶部「本地放开」开关在场(关),行值输入只读 + 锁提示;基线卡仍可编辑', async () => {
    const w = mountPanel({
      varName: 'env', baseline: 'qa',
      rows: [{ env: 'prod' }], caseNames: ['data-1'],
      locked: true, unlocked: false,
    })
    expect(w.find('.vdp-lockbar').exists()).toBe(true)
    expect(w.find('.vdp-lockbar').text()).toContain('过程变量')
    const rowInput = w.find('input[aria-label="data-1 env"]')
    expect(rowInput.attributes('readonly')).toBeDefined()
    expect(w.find('.vdp-rowval-note').text()).toContain('已锁定为过程变量,本数据集使用自有值')
    // 基线卡不受锁约束(共享侧编辑面)
    const baseInput = w.find('input[aria-label="基线 env"]')
    expect(baseInput.attributes('readonly')).toBeUndefined()
    w.unmount()
  })

  it('PANEL-LOCK-2: 开关 → emit toggle-unlock(name, true/false);恢复默认 → emit reset-default(name)', async () => {
    const w = mountPanel({
      varName: 'env', baseline: 'qa',
      rows: [{ env: 'prod' }], caseNames: ['data-1'],
      locked: true, unlocked: false,
    })
    await w.find('.vdp-lockbar button.vdp-lock-toggle').trigger('click')
    expect(w.emitted('toggle-unlock')![0]).toEqual(['env', true])
    await w.find('.vdp-lockbar button.vdp-reset').trigger('click')
    expect(w.emitted('reset-default')![0]).toEqual(['env'])
    w.unmount()
  })

  it('PANEL-LOCK-3: unlocked = true → 行值可编辑,开关呈开态', async () => {
    const w = mountPanel({
      varName: 'env', baseline: 'qa',
      rows: [{ env: 'prod' }], caseNames: ['data-1'],
      locked: true, unlocked: true,
    })
    expect(w.find('button.vdp-lock-toggle').text()).toContain('收回')
    expect(w.find('input[aria-label="data-1 env"]').attributes('readonly')).toBeUndefined()
    w.unmount()
  })

  it('PANEL-LOCK-4: 未锁定变量 → 无锁面(开关/提示均不在场)', async () => {
    const w = mountPanel({
      varName: 'amount', baseline: '100',
      rows: [{ amount: '1' }], caseNames: ['data-1'],
      locked: false, unlocked: false,
    })
    expect(w.find('.vdp-lockbar').exists()).toBe(false)
    w.unmount()
  })
})
```

- [ ] **Step 2: 跑测试确认失败**

Run: `cd src/gimbal-platform/frontend && npx vitest run src/components/dataset/__tests__/VariableDetailPanel.test.ts`
Expected: FAIL(`vdp-lockbar` 不存在)

- [ ] **Step 3: 面板实现**

**(a) props/emits**(L166-186 扩):

```typescript
const props = defineProps<{
  varName: string
  /* …既有字段不动… */
  /** 声明锁定(config.var_locks 在场)— 行值区默认只读 */
  locked?: boolean
  /** 本数据集已本地放开(var_unlocks 在场)— 行值恢复可编辑 */
  unlocked?: boolean
}>()

const emit = defineEmits<{
  'set-baseline': [value: string]
  'set-cell': [rowIndex: number, value: string]
  'jump': [ref: GridVarColumn]
  'toggle-unlock': [name: string, on: boolean]
  'reset-default': [name: string]
}>()
```

**(b) 顶部锁条**(vdp-head 之后插入):

```html
    <!-- 锁条(var-lock spec §2):过程变量标识 + 本地放开开关 + 恢复默认;
         基线卡不受锁约束(锁的是数据集编辑面姿态,不是共享侧声明) -->
    <div v-if="locked" class="vdp-lockbar">
      <span class="vdp-lock-tag">🔒 过程变量 · 默认用共享侧配置</span>
      <button
        type="button"
        class="vdp-linkbtn vdp-lock-toggle"
        @click="emit('toggle-unlock', varName, !unlocked)"
      >{{ unlocked ? '收回本地放开' : '本数据集放开编辑' }}</button>
      <button
        v-if="rows.some(r => Object.prototype.hasOwnProperty.call(r, varName)) || unlocked"
        type="button"
        class="vdp-linkbtn vdp-reset"
        @click="emit('reset-default', varName)"
      >恢复默认(清全部覆盖)</button>
    </div>
```

**(c) 行值区**(L97-111):卡片标题后加说明,输入加 readonly:

```html
      <div class="vdp-card-title">行值</div>
      <div v-if="locked && !unlocked" class="muted vdp-rowval-note">
        已锁定为过程变量,本数据集使用自有值 — 可「本数据集放开编辑」或「恢复默认」回到共享侧配置
      </div>
```

行值 input 加 `:readonly="locked && !unlocked"`。

**(d) 样式**:

```css
.vdp-lockbar {
  display: flex; align-items: center; gap: 10px; flex-wrap: wrap;
  border: 1px solid #e2e8f0; background: #f8fafc;
  border-radius: 6px; padding: 6px 10px; font-size: 11px;
}
.vdp-lock-tag { color: #64748b; }
.vdp-rowval-note { font-size: 11px; color: #b45309; }
```

- [ ] **Step 4: DataSetEditor 接线**(面板调用 L105-120):

```html
      <VariableDetailPanel
        v-if="varChoice !== 'all' && currentVarColumn"
        :key="varChoice"
        :var-name="currentVarColumn.varName"
        :baseline="currentVarColumn.baseline"
        :baseline-structured="currentVarColumn.baselineStructured"
        :baseline-json="currentVarColumn.baselineJson"
        :refs="currentVarColumn.refs"
        :rows="rows"
        :case-names="caseNames"
        :step-contexts="stepContexts"
        :query-config="queryConfig"
        :locked="isLockDeclared(currentVarColumn.varName)"
        :unlocked="varUnlocks.includes(currentVarColumn.varName)"
        @set-baseline="onPanelBaseline"
        @set-cell="onPanelCell"
        @jump="jumpToRef"
        @toggle-unlock="(name, on) => on ? unlockVar(name) : relockVar(name)"
        @reset-default="resetVarDefault"
      />
```

(单变量面板的 `onPanelCell` 走 `onCellInput`,Task 5 的守卫已覆盖锁定态写路径。)

- [ ] **Step 5: 跑测试确认通过 + varlock/varview 回归**

Run: `cd src/gimbal-platform/frontend && npx vitest run src/components/dataset/__tests__/VariableDetailPanel.test.ts src/views/__tests__/DataSetEditor.varlock.test.ts src/views/__tests__/DataSetEditor.varview.test.ts && npx vue-tsc --noEmit`
Expected: 全 PASS / 0 类型错

- [ ] **Step 6: Commit**

```bash
git add src/gimbal-platform/frontend/src/components/dataset/VariableDetailPanel.vue \
        src/gimbal-platform/frontend/src/views/DataSetEditor.vue \
        src/gimbal-platform/frontend/src/components/dataset/__tests__/VariableDetailPanel.test.ts
git commit -m "feat(frontend): 单变量面板锁定态 — 本地放开开关/行值只读/恢复默认,基线卡不受锁约束

Co-Authored-By: Claude Code <noreply@anthropic.com>"
```

---

### Task 7: 全量回归 + 引擎未动证明

**Files:**
- 无新改动(只跑测试;发现回归回到对应任务修)

**Interfaces:**
- Consumes: Task 1-6 全部。
- Produces: 全绿基线(spec §6「dispatcher 既有测试原样通过 = 引擎未动的回归证明」)。

- [ ] **Step 1: 后端全量**

Run: `cd src/gimbal-platform/backend && python -m pytest -q`
Expected: 全 PASS,零修改(`test_run_*` / dispatcher 黄金等价与注入通路测试原样通过 — 引擎零改动的证明;若任何 dispatcher 测试失败,说明实现越界碰了运行链,回 spec §4.3 检查)

- [ ] **Step 2: 前端全量 + 类型**

Run: `cd src/gimbal-platform/frontend && npx vitest run && npx vue-tsc --noEmit`
Expected: 全 PASS / 0 类型错

- [ ] **Step 3: 汇报(无 commit,除非修了回归)**

汇报全量结果;如有失败,定位回所属任务修复后重跑本任务。

---

## Self-Review 记录(计划自查,已执行)

1. **Spec 覆盖**:§2 语义表逐行 → 网格沉底/灰化(Task 5 LOCK-1)、本地放开(Task 5 LOCK-3 + Task 6)、历史覆盖提示(Task 5 LOCK-2)、恢复默认(Task 5 LOCK-4 + Task 6)、CSV 导出排除(Task 5 LOCK-6)、导入跳过提示(Task 4 + Task 5 LOCK-7)、运行零改动(Task 7)、共享侧开关(Task 3)。§3 数据模型 → Task 1。§4.1 后端三件 → Task 1/2/7。§4.2 前端五件 → Task 3/4/5/6。§5 五条 → Global Constraints。§6 测试面 → 各任务 Step 1 + Task 7。无缺口。
2. **占位符扫描**:无 TBD/TODO;所有代码块为完整可落代码(`/* …原 map 体不动… */` 是「保留既有代码」标记,非待补内容)。
3. **类型一致性**:`var_unlocks`(Python)/`varUnlocks`(wire)/`var_unlocks`(前端 TS 属性)三态命名与 alias 全程一致;`rename_in_list` 定义(Task 2 Step 3)与调用(Step 4a/4c)签名一致;`skippedLocked`(Task 4 产出 = Task 5 LOCK-7 消费)一致;`toggle-unlock`/`reset-default` 事件名 Task 6 内产出/接线一致。
4. **已知裁量**(执行时不再问):① Task 4 `skippedLocked` 必填与否由 vue-tsc 实测决定(Step 4 已写分支);② Task 5 LOCK-8 的 store mock 取法备了 `(w.vm as any).store` 不可达时的替代;③ 锁钮/徽标用 emoji 字形(🔒/🔓),与文件内既有 inline-svg 风格并存 — 展示层,可后续统一。
